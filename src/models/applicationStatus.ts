export enum ApplicationStatus {
  InProgress = "in progress",
  Hired = "hired",
  Rejected = "rejected",
}

export enum ApplicationStatusEnum {
  InProgress = "in progress",
  Hired = "hired",
  Rejected = "rejected",
}
